---
import { site, freeClass } from "../data/content.js";
---

<section id="free-class" class="free-class">
  <div class="container">
    <p class="eyebrow reveal">{freeClass.eyebrow}</p>
    <div class="video-frame reveal">
      <iframe
        src={`https://www.youtube-nocookie.com/embed/${site.freeClassVideoId}`}
        title="The Beauty of Human Desire — free class with Rav Yisrael Frenkel"
        loading="lazy"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowfullscreen
      ></iframe>
    </div>
  </div>
</section>

<style>
  .free-class {
    background: var(--paper);
  }
  .video-frame {
    margin-top: 1.5rem;
    position: relative;
    width: 100%;
    padding-top: 56.25%;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 20px 50px rgba(43, 36, 32, 0.16);
    border: 1px solid var(--line);
  }
  .video-frame iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
  }
</style>
